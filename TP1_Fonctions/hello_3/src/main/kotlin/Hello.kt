//2. Learn why almost everything has a value
// We need to run the code to have the answer "False"

fun main(args: Array<String>) {
   val temperature = 10
    val isHot = if (temperature > 50) true else false
    println(isHot)
}